import React from "react"
import BookingForm from "../../components/BookingForm"

const BookingPage = () => {
    return (
        <section className="booking container">
            <h2 className="visually-hidden">Booking</h2>
            <BookingForm />
        </section>
    )
}

export default BookingPage
